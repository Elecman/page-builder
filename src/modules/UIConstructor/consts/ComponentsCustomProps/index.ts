import type {CSSProperties} from 'react';

export const defaultStyleProps: CSSProperties = {
	background: '#fff'
};

export const defaultLayoutProps: CSSProperties = {
	display: 'flex',
	flexDirection: 'row',
	flexWrap: 'wrap'
};
