import {css} from '@emotion/react';

export const baseStyle = css({
  padding: 4,
  background: '#fff',
  maxHeight: 32,
  borderBottom: '1px solid #f0f0f0',
  boxShadow: '0px 2px 4px -4px rgba(34, 60, 80, 0.18)',
});
